const messageMap = {
    badCardIndex: 'Card index is larger than size of deck!',
    badDeal: 'The hands and handsize requested cannot be dealt from the available cards!',
    badHandIndex: 'Hand index is larger than size of hands or card index is larger than hand size!'
};

export default messageMap;
