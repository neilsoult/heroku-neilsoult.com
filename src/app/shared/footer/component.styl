@require '../layout/global.variables'

:host
    align-items center
    background $primary-dark
    color $white
    display flex
    justify-content center
    overflow auto
    relative()
    text-align center
    widthHeight 100% 5vh
    & > *
        flex 1
