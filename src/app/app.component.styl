@require './shared/layout/global.variables'

:host
    display block
    widthHeight 100%
    app-footer
        height 5vh
    app-header
        height 15vh
    .content
        display flex
        margin 0 auto
        min-height 80vh
        min-width 800px
        padding 0 5%
